To run this code challenge solution execute the main method in Solution.java.

It will run against the data in src/main/resources/MOCK_DATA.json by default.  This can be changed by editing the main method.